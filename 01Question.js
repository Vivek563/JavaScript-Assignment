// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

let myname = "Vivek Maurya";
let tag;
let insta = null;
let flag = true;

console.log(`Name is ${myname} and type is ${typeof myname}`);
console.log(`Tag is ${tag} and type is ${typeof tag}`);
console.log(`Name is ${insta} and type is ${typeof insta}`);
console.log(`Flag is ${flag} and type is ${typeof flag}`);